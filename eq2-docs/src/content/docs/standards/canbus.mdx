---
title: CANBus Standards
description: CANBus Standards for Equinox 2
---

CAN Standards v3.0
CANBus messages will not have responses, only reuests.
## CAN Bus Standards
### Header
The header specifies three key segments:

| GroupID | Device | Command ID |
| ------- | ------ | ---------- |
| 2 bits  | 5 bits | 4 bits |

#### GroupID
Specifies the network segment of devices (group) intended to receive the message. A group typically has a size of 32 devices. There are 4 possible groups within the network.

Due to collisions with MyActuator namespace, group 0x1 is unusable. Only Groups 0x00, 0x02 and 0x03 are available. 

#### Device
Specifies the device within the group that is sending or receiving the message. 

#### Command ID
Indicates the type of message being sent and the intended outcome on the machine.

These are:

| ID | Command | Sent From | Unit Size (bits) | Description | 
| --- | ------- | --- | ------ | ----------- |
| 0x00 | Ping | Either | 0 | Checks whether a device is connected, also used as heartbeat payload |
| 0x01 | EStop   | Master | 0 | Stops the device completely |
| 0x02 | TxBool  | Master | 8 | Sends a boolean value |
| 0x03 | TxInt   | Master | 16 | Sends an integer value |
| 0x04 | TxFloat | Master | 32 | Sends a floating point value |
| 0x05 | TxData  | Master | 64 | Sends an arbitrary byte buffer |
| 0x06 | BrBool  | Slave | 8 | Receives a boolean value |
| 0x07 | BrInt   | Slave | 16 | Receives an integer value |
| 0x08 | BrFloat | Slave | 32 | Receives a floating point value |
| 0x09 | BrData  | Slave | 64 | Broadcasts an arbitrary byte buffer |
 
Each packet will include between 1 and 'n' inclusions of the datatype in little endian order.

For example, a TxInt has a size of 16 bits. This means that up to 4 signed integers can be packed in the packet as follows:

| Offset| Byte 1 | Byte 2 |
| ------ | --------------- | --------------- |
| 0x00   | A (Int16)[0] | A (Int16)[1] | 
| 0x02   | B (Int16)[0] | B (Int16)[1] |
| 0x04   | C (Int16)[0] | C (Int16)[1] |
| 0x06   | D (Int16)[0] | D (Int16)[1] |

Messages are intended to be asynchonous and not involve transactions. This requires a listening thread to handle broadcast messages separately to the sending thread.


### Excavator
- Uses TxInt to set the actuator velocity for the excavator arms and bucket motors. Motor is selected by device id, with netmasking selecting the parent device.

Message Type: TxInt

| Offset| Byte 1 | Byte 2 |
| ------ | --------------- | --------------- |
| 0x00   | Motor Speed (Int16)[0] | Motor Speed (Int16)[1] | 
| 0x02  | - | - |
| 0x04   | - | - |
| 0x06   | - | - |



### Gimbal
- Sets the Tilt position and Pan speed of a gimbal camera.

Message Type: TxInt

| Offset| Byte 1 | Byte 2 |
| ------ | --------------- | --------------- |
| 0x00   | Pan Speed (Int16)[0] | Pan Speed (Int16)[1] | 
| 0x02   | Tilt Pos (Int16)[0] | Tilt Pos (Int16)[1] |
| 0x04   | - | - |
| 0x06   | - | - |

### PDB

- Firmware will broadcast status messages every 1 second by default. Each channel will present with a seperate device id. For example, a switchboard with 8 channels may occupy the device ids of 0 to 7. It is recommended to use the upper bits of the device id to specify which switchboard is selected. In this scenario, the device id netmask would be 0x1c.

The message shall contain:

Message Type: TxInt

| Offset | Byte 1 | Byte 2 |
| ------ | ------ | ------ |
| 0x00 | Voltage (Int16)[0] | Voltage (Int16)[1] | 
| 0x02 | Current (Int16)[0] | Current (Int16)[1] | 
| 0x04 | Temperature (Int16)[0] | Temperature (Int16)[1] | 
| 0x06 | - | - | 



- Toggle channel similarly will switch the state of a channel to a boolean value:

Message Type: TxBool

| Offset | Byte 1 | Byte 2 |
| ------ | ------ | ------ |
| 0x00 | State (Bool) | - |
| 0x02 | - | - |
| 0x04 | - | - |
| 0x06 | - | - | 


### Science
- Set Stepper (1, 2 or 3)
- DC (set)
- RequestDP (Each Sensor, DC Speed, Stepper POS)

- Stepper positions (steps) are set by referencing the stepper motor as a function of the device id. Netmasking can be used to select specific control devices (see PDB)

Message Type: TxInt

| Offset | Byte 1 | Byte 2 |
| 0x00 | Steps (Int16)[0] | Steps (Int16)[1] | 
| 0x02 | - | - |
| 0x04 | - | - |
| 0x06 | - | - | 


- Set DC Motor presents the same interface as Stepper motors only segmented by the device id.

Message Type: TxInt

| Offset | Byte 1 | Byte 2 |
| 0x00 | Speed (Int16)[0] | Speed (Int16)[1] | 
| 0x02 | - | - |
| 0x04 | - | - |
| 0x06 | - | - | 

Message Type: 

### Arm - End Effector
- SetServoSpeed
    - Poke
    - Open / Close


#### Invalid Command IDs (due to MyActuators)
The following CAN IDs are unavailable due to limitations with Coexisting MyActuator Motors.

Group ID 0x1 is reserved.

0b01001000000 0b0101000000
0b01001000001 0b0101000001
0b01001000010 0b0101000010
0b01001000011 0b0101000011
0b01001000100 0b0101000100
0b01001000101 0b0101000101
0b01001000110 0b0101000110
0b01001000111 0b0101000111
0b01001001000 0b0101001000
0b01001001001 0b0101001001
0b01001001010 0b0101001010
0b01001001011 0b0101001011
0b01001001100 0b0101001100
0b01001001101 0b0101001101
0b01001001110 0b0101001110
0b01001001111 0b0101001111